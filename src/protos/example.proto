syntax = "proto3";

// Greeter service
service Greeter {
  // SayHello method
  rpc SayHello (HelloRequest) returns (HelloReply) {
  }
}

// Hello request
message HelloRequest {
  // Name
  string name = 1;
}

// Hello reply
message HelloReply {
  // Message text
  string message = 1;
}
